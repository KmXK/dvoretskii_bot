# Фича: Подсчёт реакций на видео

## Описание

Автоматический подсчет реакций на видео, отправленные пользователями через ссылки (TikTok, Instagram Reels, YouTube, Pinterest).

## Как работает

1. **Пользователь отправляет ссылку** на видео (TikTok/Reels/YouTube/Pinterest)
2. **Бот скачивает и отправляет видео** в чат
3. **Система сохраняет связь** между сообщением пользователя (ссылка) и сообщением бота (видео)
4. **Реакции учитываются на оба сообщения:**
   - Реакция на сообщение со ссылкой → засчитывается автору
   - Реакция на сообщение с видео → засчитывается автору

## Особенности

### Дедупликация
Если один и тот же пользователь поставил реакцию и на ссылку, и на видео — это считается **только один раз**.

**Пример:**
- Пользователь A отправил TikTok
- Пользователь B поставил 👍 на сообщение со ссылкой
- Пользователь B поставил ❤️ на сообщение с видео
- **Результат:** Пользователю A засчитывается **1 уникальный пользователь** (B), 2 реакции

### Поддерживаемые платформы
- TikTok
- Instagram Reels
- YouTube
- Pinterest

## Команды

### `/video_stats` или `/reactions_top`
Показывает топ-10 пользователей по количеству реакций на их видео.

**Формат вывода:**
```
📊 Топ по реакциям на видео:

🥇 1. Иван
   ├ Видео: 15
   ├ Уникальных пользователей: 42
   └ Всего реакций: 87

🥈 2. Мария
   ├ Видео: 12
   ├ Уникальных пользователей: 38
   └ Всего реакций: 65
```

## Технические детали

### Модель данных
```python
@dataclass
class VideoReaction:
    chat_id: int                      # ID чата
    user_id: int                      # ID автора (кто кинул ссылку)
    user_message_id: int              # ID сообщения со ссылкой
    bot_message_id: int               # ID сообщения с видео от бота
    video_type: str                   # tiktok/reels/youtube/pinterest
    reactions: dict[int, set[str]]    # user_id -> set of emojis
```

### Хранилище
Данные сохраняются в `db.json` в поле `video_reactions`.

### Обработчики
1. **ReactionCounterHandler** — отслеживает реакции
2. **DownloadHandler** — регистрирует связь при отправке видео
3. **VideoStatsHandler** — показывает статистику

## Миграция
Версия базы данных обновлена до `9` для поддержки нового поля `video_reactions`.

## Примеры использования

### 1. Пользователь отправляет TikTok
```
Алексей: https://tiktok.com/@user/video/123
Бот: [отправляет видео]
```

### 2. Другие пользователи ставят реакции
```
Иван ставит 👍 на сообщение Алексея
Мария ставит ❤️ на видео от бота
Петр ставит 🔥 на оба сообщения
```

### 3. Подсчет
- Алексею засчитывается: **3 уникальных пользователя** (Иван, Мария, Петр)
- Всего реакций: **4** (👍 + ❤️ + 🔥×2, но Петр считается один раз как пользователь)

### 4. Статистика
```
/video_stats

📊 Топ по реакциям на видео:

🥇 1. Алексей
   ├ Видео: 1
   ├ Уникальных пользователей: 3
   └ Всего реакций: 4
```

## Обновления
- **2026-02-18:** Первая версия фичи
