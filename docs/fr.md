логика работы команды /fr (feature request):

/fr {текст} - добавить новый фича-реквест
текст записывается в базу, автор определяется автоматически
приоритет по умолчанию: 5 (самый низкий), статус: Открыт

/fr list - показать список открытых фича-реквестов
список отсортирован по приоритету (1 - наивысший)
каждая запись отображается с эмодзи приоритета: 🔴1 🟠2 🟡3 🔵4 ⚪5
inline-кнопки для фильтрации по статусам: Все, Завершённые, Отклонённые, Открытые, В работе, Тестирование
поддерживается пагинация

/fr {id} - посмотреть полную информацию по фича-реквесту
отображается: статус, приоритет, автор, текст, дата создания, история изменений, примечания

/fr {id} priority {1-5} - изменить приоритет фича-реквеста
1 — наивысший, 5 — самый низкий
проверка: число от 1 до 5, иначе ошибка
изменять может автор или админ

/fr {id} note {текст} - добавить примечание к фича-реквесту
примечания накапливаются (каждая новая команда добавляет к предыдущим)
добавлять может автор или админ

/fr done {id1} {id2} ... - пометить фича-реквест(ы) как выполненные
/fr deny {id1} {id2} ... - отклонить фича-реквест(ы)
/fr reopen {id1} {id2} ... - переоткрыть фича-реквест(ы)
/fr inprogress {id1} {id2} ... - пометить как "в работе"
/fr testing {id1} {id2} ... - пометить как "на тестировании"

статусы:
- Открыт (OPEN) — начальный статус
- В работе (IN_PROGRESS) — фича в процессе реализации
- На тестировании (TESTING) — фича реализована, тестируется
- Завершён (DONE) — фича выполнена
- Отклонён (DENIED) — фича отклонена

при смене статуса запись добавляется в историю изменений с меткой времени
изменять статус может автор фича-реквеста или админ
при попытке установить текущий статус повторно выводится соответствующая ошибка

веб-режим (Mini App):
- список фича-реквестов с фильтрацией по статусу и поиском
- сортировка: по приоритету, ID, статусу, автору, и комбинированные (приоритет+статус, статус+приоритет)
- при нажатии на реквест открывается карточка
- в карточке можно: менять статус, приоритет, добавлять примечания
- кнопка "Сохранить" внизу карточки для применения изменений
- отображается история изменений и все примечания

алиасы команды: /fr, /featurerequest
