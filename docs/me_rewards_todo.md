### Профиль человека

/me - команда которая позволяет человеку просмотреть свой профиль, вывод состоит из блоков и кнопок

Блоки:
- достижения - в формате строки состоящей из иконок достижений

Кнопки:
- Достижения - переключает текущее сообщение на список достижений с пагинацией

Подробнее о достижениях - текущее сообщение редактируется и показывает список достижений с пагинацией в формате
id. медалька - наименование

Есть кнопка "← Назад" которая возвращает к общему профилю

### Достижения

/rewards - посмотреть общий список достижений с пагинацией в формате
id. медалька - наименование
для динамических достижений рядом показывается текущий владелец: `→ @username`

... add - добавить новое достижение (сессия)
бот спрашивает наименование/описание
пользователь отвечает
бот спрашивает медальку
пользователь отправляем смайлик (смайлики могут быть из кастомных телеграмм наборов)

... add {название} {эмоджи} - добавить достижение одной командой
эмоджи может быть стандартным или кастомным телеграм-эмоджи, должен быть последним словом

... remove {id} - удалить достижение, удаляет так же у тех кому оно присвоено (не работает для динамических)

... {id медали} present {id пользователя/username} ... - вручить медаль (не работает для динамических)

... {id медали} take {id пользователя/username} ... - изъять медаль (не работает для динамических)

### Динамические достижения

Автоматические достижения, которые пересчитываются каждый час.
Владелец определяется по метрикам/данным из БД. Достижение в любой момент принадлежит одному человеку.
При смене лидера достижение автоматически переходит.

Список динамических достижений:
- 💬 **Болтун** — больше всего сообщений (метрика: `bot_messages_total{action_type="chat"}`)
- 🃏 **Картёжник** — больше всего покерных раздач (метрика: `poker_hands_total`)
- 🎯 **Шулер** — лучший винрейт в покере, мин. 10 раздач (computed: wins/total)
- 🐵 **Обезьяний рабовладелец** — больше всего обезьянок (из БД: `user.monkeys`)
- 🎲 **Главный лудик** — больше всего игр в казино (метрика: `casino_games_total`)
- 💸 **Спонсор** — больше всего поставлено обезьянок (метрика: `casino_monkeys_bet_total`)

Реализация: `steward/dynamic_rewards.py`
- `DynamicRewardDef` — описание динамического достижения
- `DynamicRewardChecker` — asyncio-таска, пересчитывает каждые 3600 секунд
- `ensure_dynamic_rewards_exist()` — вызывается при старте бота, создаёт записи в БД
- Поле `Reward.dynamic_key` — связь между записью в БД и определением
- Ручные present/take/remove заблокированы для динамических достижений

### Список событий

для каждого чата свои события
очень похоже на fq 

/todo - показать список событий для этого чата с пагинацией

/todo {текст} - добавить событие

... remove {id} - удалить событие

... done {id} - завершить событие
бот завершает событие
бот спрашивает не хотите ли вы добавить достижение (кнопки пропадают после нажатия)
если нет то все
если да то бот спрашивает про название достижения, медаль и кому присвоить(можно нескольким людям)
